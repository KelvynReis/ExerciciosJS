const calculo = (valor1, valor2) =>{
    const soma = valor1 +valor2
    const subtracao = valor1 - valor2
    const divisao = valor1 / valor2

    console.log(`A resposta da soma é ${soma}, subtração: ${subtracao}, divisão: ${divisao}`);
}

calculo(10,5)